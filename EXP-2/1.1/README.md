# EXP-2/1.1 — Redux Toolkit + Memoized Selectors

This project demonstrates centralized state management using Redux Toolkit, normalized state shapes, and memoized selectors for derived state.

Run:

```bash
cd EXP-2/1.1
npm install --legacy-peer-deps
npm run dev
```
