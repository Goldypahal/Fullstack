# EXP-2/1.2 — Memoized Selectors and Performance

This experiment demonstrates memoized selectors, selector factories, and rendering optimizations.

Run:

```bash
cd EXP-2/1.2
npm install --legacy-peer-deps
npm run dev
```
